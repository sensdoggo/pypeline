[project]
name = "core"
version = "0.1.0"
description = "core types, base classes, utility functions for pypeline"
readme = "README.md"
license = 'GPL-3.0-or-later'
license-files = ["LICEN[CS]E*"]
requires-python = ">=3.14"
dependencies = [

]
